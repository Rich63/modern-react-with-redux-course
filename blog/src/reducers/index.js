import { combineReducers } from "redux";

// Reducers
export default combineReducers({
  // dummy reducer to get the application running.
  replaceMe: () => "This is a dummy reducer, this has to be replaced!"
});